import { UssSnapScroller } from "./uss-class-snap-scroller.js";

export class UssElasticScroller extends UssSnapScroller {


    //The constructor assumes that the options object is valid.
    //All the checks are done before invoking it.
    constructor(container, options) {
        super(container, options);
    
    }
}